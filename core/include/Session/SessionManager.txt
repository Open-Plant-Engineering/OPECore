i am having GitWrapper class and JsonCRUD Class with following hpp

#ifndef JSONCRUD_HPP
#define JSONCRUD_HPP

#include <string>
#include <nlohmann/json.hpp>

using json = nlohmann::json;

class JsonCRUD {
private:
    std::string filename;
    json data;

    void load();
    void save();

public:
    explicit JsonCRUD(const std::string& file);

    // CRUD operations
    void create(const std::string& key, const json& value);
    json read(const std::string& key);
    void update(const std::string& key, const json& value);
    void remove(const std::string& key);

    // Utility
    void printAll();
};

#endif // JSONCRUD_HPP

#ifndef GITWRAPPER_HPP
#define GITWRAPPER_HPP

#include <string>
#include <vector>
#include <git2.h>

class GitWrapper {
public:
    explicit GitWrapper(const std::string& repoPath);
    ~GitWrapper();

    // Core Git operations
    bool gitPull(const std::string& remote = "origin", const std::string& branch = "main");
    bool gitPush(const std::string& remote = "origin", const std::string& branch = "main");
    bool gitMerge(const std::string& branch);
    bool gitCreateBranch(const std::string& branch);
    bool gitRenameBranch(const std::string& oldName, const std::string& newName);
    bool gitRebase(const std::string& branch);
    bool gitStash(const std::string& message = "WIP");
    bool gitReset(const std::string& commit = "HEAD~1", const std::string& mode = "hard");
    bool gitDeleteBranch(const std::string& branch);

    // Utility
    std::vector<std::string> getModifiedFiles();
    bool checkoutBranch(const std::string& branch);
private:
    git_repository* repo;
    std::string repoPath;
};

#endif // GITWRAPPER_HPP

I have create one new class Session Manager to do following things:
1. It should have method to clone the project(Repository)
2. It should allow to create new json file with unique ID in the same repository folder
3. It should allow to modify attribute available in json.
4. Whenever we modify the attribute it must create a new file with same exisiting file name but with some prefix e.g. Working.
5. Whenever the work is finished and user try to commit it should get the work and modify the data which are changed by the user only if it's not updated because of getwork or git pull. Basicall i want to avoid issues due to merge conflict so we are creating json file has changes with some prefix.
6. once json data modification is done. use must commit the code.

if you have any questions or query please ask and give code:

- Language + bindings: it will be exposed to python in future for UI integration but as of now it should be commandline python
- Repository + paths: jsons will like in certain paths within repository but logic for the same we will decide in next phase. and also note that there will be multiple files available in repo but at a time user will update only one file.
- filenames + “working” copies is fine for me but instade of working write computer hostname and yes Compare fields, Apply only changed fields from the working file back into the original, Then delete the working file.
- Merge/“conflict avoidance” logic:
1. Git pull will not disturb any working copy. It will update original file only. 
2. Hence when we see the updated data for the attribute in original git pull file and same data is changed by session then use's changes for that file will be rejected.
3. i guess merge conflict will be manuall between working copy, original copy after git pull based on what data updated before and after git pull as well as working copy.
- Git responsibilitiesL planning is to manipulate files only and call our app to manage git pull, add an commit from wrapper but if there is something required to be update and use from git then we will update gitwrapper class.
i need both cpp and hpp such that it will go in production.

if you have any extra question please ask. and write modular code.